package cli;

import annotation.Draft;

@Draft
public class CommandParser {

    @Draft
    public static long parse(long rawStringPointer) {
        // TODO: Tokenize the raw primitive.string pointer by splitting on spaces
        // TODO: Return an off-heap struct pointer representing the parsed Command
        return 0L;
    }
}
