package spatial;

public class CoordinateConversion
{
    // this is where the coordinate system is being converted into a float to int32fp32 or smth, and so on...
    // this is where the coordinate is being pushed to the gpu i suppose? or the gpu does it at the same time

    public CoordinateConversion()
    {

    }
}
